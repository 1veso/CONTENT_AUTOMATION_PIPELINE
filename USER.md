# USER.md — About the Operator

- **Name:** Benja (trendivalux@gmail.com)
- **Timezone:** Europe/Berlin
- **OS:** Windows 11 Pro (PowerShell 7+, Bash via WSL/Git Bash)
- **Workspace:** `C:\CONTENT_PIPELINE\`

## What I Do

I run three production content pipelines (R55, R57, R61) for **Provinzial / Geier & Ayhan** — German insurance brand campaign. Daily cinematic ad videos, daily static social images, and YouTube Shorts cut from long-form. All scheduled into Blotato across TikTok, Meta, Instagram.

## How I Want to Work

- Brief, no fluff. Lead with the answer.
- Confirm cost before any paid API call. Always.
- Confirm before any destructive action (delete, overwrite, force-push).
- Use existing tools in `R57_content_engine/tools/` and `R61_video_pipeline/tools/`. Never write throwaway scripts at repo root.
- Read `obsidian-brain/_index.md` for project state — that's the source of truth for what's queued, what's shipped, what's blocked.
- Honor the auto-memory at `C:\Users\benja\.claude\projects\C--CONTENT-PIPELINE\memory\` — that's the long-term record of corrections and project state.

## Preferences

- Show me the cost before generating. Wait for explicit approval.
- Schedule in Blotato. Never post immediately.
- New version = new path. Never overwrite a render.
- When a human gate is reached, ping me on Telegram with clear options. Don't proceed on assumption.
- For status checks across pipelines, default to checking Airtable base `appC3HqG42ftswOvw` (R57 table `tblnpiwNYF3zJXm9Q`, R61 table `tbl1hd8yprLTZia4c`).
